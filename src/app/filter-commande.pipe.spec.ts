import { FilterCommandePipe } from './filter-commande.pipe';

describe('FilterCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
